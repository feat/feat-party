import { defineMessages } from '@/services/intl';

export default defineMessages({
  fetchErrorHint: {
    id: 'party.archive-viewer.fetch-error-hint',
    defaultMessage: 'Error: Failed to fetch data',
  },
  retryLabel: {
    id: 'party.archive-viewer.retry-label',
    defaultMessage: 'Retry',
  },
});
