import { createAction } from 'redux-actions';

export const appInitialSuccess = createAction('GLOBAL/APP_INITIAL_SUCCESS');
export const appInitialFailure = createAction('GLOBAL/APP_INITIAL_FAILURE');
