import { defineMessages } from '@/services/intl';

export default defineMessages({
  newDemandLabel: {
    id: 'exc.demand-dash.new-demand-label',
    defaultMessage: 'New Demand',
  },
  noDemandsLabel: {
    id: 'exc.demand-dash.no-demands-hint',
    defaultMessage: 'No demands',
  },
});
