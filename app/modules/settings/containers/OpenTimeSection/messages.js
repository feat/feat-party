import { defineMessages } from '@/services/intl';

export default defineMessages({
  openTimeSectionTitle: {
    id: 'settings.open-time-section.title',
    defaultMessage: 'OpenTime',
  },
  // --- openTime ---
  openTimeUpdated: {
    id: 'settings.requests.open-time-updated',
    defaultMessage: 'Open Time updated',
  },
  openTimeUpdateFailed: {
    id: 'settings.requests.open-time-update-failed',
    defaultMessage: 'Failed to update open time',
  },
    
});
